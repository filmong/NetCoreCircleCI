version: 2
jobs:
  # job name is "one:"
  build:
    docker:
      - image: microsoft/dotnet:2.1-runtime
    steps:
      - checkout
      - run: find
      - run: dotnet restore
      #- run:  
          # name: Build
          # command: dotnet build
      
